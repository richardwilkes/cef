package cef

import (
{{- range .Headers}}
	// #include "{{.}}"
{{- end}}
	"C"
	"unsafe"
)
{{- range .Types}}

// {{.GoName}} ({{.Name}})
{{- $comment := .Position.Comment}}{{if $comment}}
{{$comment}}
{{- end}}
type {{.GoName}} {{.RefersTo}}
{{- end}}
